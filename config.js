// Backend removed — clear the API base URL to avoid accidental requests.
// Set to empty string so client code can detect absence of a backend.
window.FOODSHARE_API_BASE_URL = window.FOODSHARE_API_BASE_URL || '';